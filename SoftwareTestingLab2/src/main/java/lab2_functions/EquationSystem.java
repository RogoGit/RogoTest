package lab2_functions;

import static java.lang.Math.pow;
import static lab2_functions.AdvancedFunctions.*;
import static lab2_functions.BasicFunctions.*;

public class EquationSystem {

   /* public static Double trigFunction(Double x, Double precision) {
        return (((((pow(tan(x, precision),2)) + (tan(x,precision) + cos(x,precision)))
                - cot(x,precision)) * tan(x, precision)) * (sec(x, precision) + sin(x, precision)));
    }

    public static Double logFunction(Double x, Double precision) {
        return (((((pow(log_5(x, precision),2)) * ln(x,precision)) - log_2(x,precision))
                * (log_3(x, precision) + ln(x, precision))) + (log_2(x, precision)
                * ((log_2(x,precision) * log_2(x, precision)) * log_2(x, precision))));
    }

    public static Double computeFunction(Double x, Double precision) {
        if (x <= 0) return trigFunction(x, precision);
        return logFunction(x, precision);
    }*/

}
